# Base URL of the backend API. Falls back to http://localhost:3000/api
# in src/services/api.ts if this isn't set, so this file is optional for
# local development against the default backend port.
VITE_API_BASE_URL=http://localhost:3000/api
